name = "dawah-site"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# Only non-sensitive plaintext vars go here.
# Firebase keys and ImgBB key must be added as Secrets
# in Cloudflare Dashboard → Settings → Environment Variables → Type: Secret
[vars]
NODE_VERSION   = "20"
PUBLIC_SITE_URL = "https://dawah-site.pages.dev"
